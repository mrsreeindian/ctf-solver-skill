# CTF Writeup: [Challenge Name]

**Event:** [CTF Event Name — e.g., bi0sCTF 2024]
**Category:** [Pwn | Rev | Crypto | Forensics | Stego | Web | Hardware]
**Points:** [e.g., 500]
**Difficulty:** [Easy | Medium | Hard | Insane]
**Solved by:** Antigravity CTF Solver

---

## 🏁 Flag

```
flag{REPLACE_WITH_ACTUAL_FLAG}
```

---

## Challenge Description

> [Paste the original challenge description here]

**Attachments:**
- `[filename]` — [brief description]

**Remote target:** `nc HOST PORT` or `http://IP:PORT`

---

## Initial Triage

```
[Paste output of: triage.sh <file>]
```

**Key observations:**
- File type: [e.g., ELF 64-bit, LSB executable]
- Protections: [e.g., NX enabled, No PIE, No canary]
- Notable strings: [e.g., found "win" function, found base64 blob]

---

## Vulnerability / Hidden Data Identified

[Describe what you found and WHY it's exploitable/interesting]

**Root cause:** [e.g., `gets()` with no bounds checking allows stack overflow]

---

## Exploitation / Extraction Process

### Step 1: [First step name]

```bash
[exact command used]
```

Output:
```
[relevant output]
```

**Analysis:** [What this told us and what we did next]

### Step 2: [Second step name]

```bash
[exact command used]
```

### Step 3: [Continue...]

---

## Exploit / Solve Script

```python
# ─── Solve Script ──────────────────────────────────────────
from pwn import *  # or: import requests, from Crypto.Util.number import *

# binary = ELF("./challenge")
# io = remote("HOST", PORT)
# io = process([binary.path])

# [paste full solve script here]

io.interactive()
```

---

## Flag

```
flag{REPLACE_WITH_ACTUAL_FLAG}
```

---

## Technique Explanation

### What Was the Vulnerability / Hidden Data?

[Explain the core concept — e.g., "A classic stack buffer overflow via `gets()` 
with no NX meant we could place shellcode directly on the stack."]

### How Did We Exploit / Extract It?

[Step-by-step conceptual explanation without raw commands. Should be understandable 
to someone who hasn't seen the challenge.]

### Mitigations / Defenses (for Pwn/Web)

| Mitigation | Status | How we bypassed it |
|---|---|---|
| NX/DEP | [Enabled/Disabled] | [e.g., used ROP chain] |
| ASLR | [Enabled/Disabled] | [e.g., leaked libc via puts] |
| Stack Canary | [Enabled/Disabled] | [e.g., leaked via format string] |
| PIE | [Enabled/Disabled] | [e.g., partial overwrite] |

---

## Key Takeaways

1. **Technique used:** [e.g., ret2libc with format string leak]
2. **New tool learned:** [e.g., `one_gadget` for single-gadget shells]
3. **Mistake made:** [e.g., forgot to account for PIE — always run checksec first]
4. **Reference:** [bi0s wiki link or CTF writeup reference]

---

*Writeup generated by Antigravity CTF Solver | wiki.bi0s.in methodology*
